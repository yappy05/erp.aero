# 🚀 Быстрый старт: Транскрибация аудио в Google Colab

## 📝 Пошаговая инструкция (простыми словами)

### Шаг 1: Откройте Google Colab
1. Перейдите на https://colab.research.google.com/
2. Войдите в свой Google аккаунт
3. Нажмите **"Файл" → "Новый блокнот"**

### Шаг 2: Включите GPU (ВАЖНО!)
1. Вверху меню выберите **"Среда выполнения" → "Изменить тип среды выполнения"**
2. В разделе **"Ускоритель аппаратного обеспечения"** выберите **"T4 GPU"**
3. Нажмите **"Сохранить"**

### Шаг 3: Загрузите аудио файл через файловую систему Colab

**Это самый важный шаг!**

1. В левой панели Colab найдите иконку **папки** 📁 (Files)
2. Нажмите на иконку **загрузки** (Upload) или просто **перетащите файл** в эту область
3. Дождитесь завершения загрузки
4. **Запомните точное имя файла** (например: `курсор.ogg`)

### Шаг 4: Скопируйте код в ячейки Colab

Откройте файл `whisper_colab_simple.py` и скопируйте каждую ячейку по порядку:

#### Ячейка 1: Установка библиотек
```python
!pip install openai-whisper
!pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
!pip install ffmpeg-python
```

#### Ячейка 2: Загрузка модели
```python
import whisper
import torch
import os

device = "cuda" if torch.cuda.is_available() else "cpu"
print(f"Используется устройство: {device}")

print("Загружаем модель Whisper Large...")
model = whisper.load_model("large", device=device)
print("✅ Модель загружена!")
```

#### Ячейка 3: Указать имя файла
```python
# ⬇️ ЗДЕСЬ УКАЖИТЕ ИМЯ ВАШЕГО ФАЙЛА ⬇️
audio_file = "курсор.ogg"  # Замените на имя вашего файла!

# Проверка файла
if os.path.exists(audio_file):
    print(f"✅ Файл найден: {audio_file}")
else:
    print(f"❌ Файл '{audio_file}' не найден!")
```

#### Ячейка 4: Транскрибация
```python
print(f"\n🎤 Начинаем транскрибацию: {audio_file}")

result = model.transcribe(
    audio_file,
    language="ru",  # русский язык
    task="transcribe",
    verbose=True,
)

print("\n✅ Транскрибация завершена!")
```

#### Ячейка 5: Вывод результатов
```python
print("\n📝 ТЕКСТ:")
print(result["text"])

# С временными метками
print("\n⏱️  С ВРЕМЕННЫМИ МЕТКАМИ:")
for segment in result["segments"]:
    start_min = int(segment['start'] // 60)
    start_sec = int(segment['start'] % 60)
    end_min = int(segment['end'] // 60)
    end_sec = int(segment['end'] % 60)
    print(f"[{start_min}:{start_sec:02d} - {end_min}:{end_sec:02d}] {segment['text']}")
```

#### Ячейка 6: Сохранение результата
```python
output_file = audio_file.rsplit('.', 1)[0] + "_transcription.txt"

with open(output_file, "w", encoding="utf-8") as f:
    f.write("ТРАНСКРИБАЦИЯ АУДИО\n")
    f.write("="*60 + "\n\n")
    f.write(f"Файл: {audio_file}\n\n")
    f.write("ТЕКСТ:\n")
    f.write(result["text"])

print(f"✅ Результат сохранен: {output_file}")

# Скачать файл
from google.colab import files
files.download(output_file)
```

## ⚠️ Важные моменты

### Проблема: "Файл не найден"
**Решение:**
1. Убедитесь, что файл загружен через файловую систему Colab (левая панель → Files → Upload)
2. Проверьте точное имя файла (с учетом регистра и расширения)
3. В ячейке 3 укажите правильное имя файла

### Проблема: "files.upload() не работает"
**Решение:** Не используйте `files.upload()`! Загружайте файл через файловую систему Colab (левая панель → Files → Upload)

### Проблема: Медленная обработка
**Решение:** Убедитесь, что GPU включен в настройках среды выполнения

## 🎯 Что делает код (пошагово)

1. **Ячейка 1**: Устанавливает нужные библиотеки (whisper, torch и т.д.)
2. **Ячейка 2**: Загружает модель Whisper Large (это займет несколько минут)
3. **Ячейка 3**: Проверяет, что ваш аудио файл существует
4. **Ячейка 4**: Обрабатывает аудио и превращает его в текст
5. **Ячейка 5**: Показывает результат на экране
6. **Ячейка 6**: Сохраняет результат в файл и скачивает его

## 💡 Примеры использования

### Для русского языка:
```python
result = model.transcribe(audio_file, language="ru")
```

### Для английского языка:
```python
result = model.transcribe(audio_file, language="en")
```

### Автоопределение языка:
```python
result = model.transcribe(audio_file, language=None)
```

### Перевод на английский:
```python
result = model.transcribe(audio_file, language="ru", task="translate")
```

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте, что GPU включен
2. Проверьте, что файл загружен через файловую систему Colab
3. Проверьте, что имя файла указано правильно
4. Убедитесь, что все ячейки выполнены по порядку


